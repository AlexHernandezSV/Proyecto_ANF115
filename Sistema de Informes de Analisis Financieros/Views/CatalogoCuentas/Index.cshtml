@model IEnumerable<Sistema_de_Informes_de_Analisis_Financieros.Models.Catalogodecuenta>

@{
    ViewData["Title"] = "Catalogo de Cuentas";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container col-20 col-md-9 mb-5">
    <div class="card border-dark mb-9">
        <div class="card-body text-dark text-center">
            <h5 class="card-title font-weight-bold">"@ViewBag.nomEmpresa"</h5>
            <p class="card-text font-weight-bold" >CATÁLOGO DE CUENTAS.</p>
        </div>
        @if (!Model.Any())
        {
            <div class="alert alert-info" style="background-color: #409bd8;">
                Favor registrar un catalogo de cuentas, puede subir su archivo en este link: 
                <a class="ml-1" style="color:#FF0000" asp-controller="Empresas" asp-action="Edit" asp-route-id="@ViewBag.idEmpresa">Ir a edición de empresa</a>
            </div>
        }
        <table class="table table-dark table-sm">
            <thead class="table-active">
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Codcuentacatalogo)
                    </th>
                    <th class="pr-2 pl-5">
                        @Html.DisplayNameFor(model => model.IdcuentaNavigation)
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        @if (!(item.Codcuentacatalogo.Equals("0")) && !(item.Codcuentacatalogo.Equals("E")) && !(item.Codcuentacatalogo.Equals("D")))
                        {
                            <td>
                                @Html.DisplayFor(modelItem => item.Codcuentacatalogo)
                            </td>
                            <td class="pr-2 pl-5">
                                @Html.DisplayFor(modelItem => item.IdcuentaNavigation.Nomcuenta)
                            </td>
                        }
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>


